# .agents

This directory is kept intentionally.

It is reserved for agent-specific local notes, adapters, or future integration files that do not belong under `skills/bg-text/agents/`.

Right now the repository does not require committed files here beyond this placeholder.
